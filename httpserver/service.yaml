apiVersion: v1
kind: Service
metadata: 
  labels:
    app: httpserver
  name: httpserver-svc
spec:  
  - port: 8080
    protocol: tcp
    targetPort: 80
selector:
  app: httpserver
type: loadBalancer